apiVersion: v1
kind: Pod
metadata:
  name: backend-pod
  labels:
    app: backend
spec:
  containers:
  - name: server
    image: nuwnjay/docker-synapse-server:v1
    # image: node
    ports:
      - containerPort: 3000
    imagePullPolicy: IfNotPresent