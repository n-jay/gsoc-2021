apiVersion: v1
kind: Pod
metadata:
  name: backend-pod
  labels:
    app: backend
spec:
  containers:
  - name: server
    image: docker-synapse-server
    #image: node
    ports:
      - containerPort: 3000
    imagePullPolicy: IfNotPresent